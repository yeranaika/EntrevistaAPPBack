<configuration>
  <!-- Consola -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- (Opcional) Archivo rotativo diario -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>build/logs/app.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>build/logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Niveles por paquete -->
  <logger name="io.ktor" level="INFO"/>
  <logger name="io.netty" level="WARN"/>
  <!-- Cambia a tu paquete raíz para ver tus logs en DEBUG -->
  <logger name="com.example" level="DEBUG"/>

  <!-- Root -->
  <root level="INFO">
    <appender-ref ref="STDOUT"/>
    <!-- Actívalo si quieres también a archivo -->
    <!-- <appender-ref ref="FILE"/> -->
  </root>
</configuration>
